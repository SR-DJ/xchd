# 石火电竞新春福袋抽奖活动 (xchd 版)

这是一个包含代码混淆和 GitHub Pages 自动部署功能的抽奖活动项目。

## 🚀 快速开始

1. 将解压后的所有文件（包括隐藏的 `.github` 文件夹）上传到您的 GitHub 仓库根目录。
2. 确保 `package.json` 位于仓库根目录。
3. 在仓库设置中开启 Actions 写入权限 (`Settings > Actions > General > Workflow permissions > Read and write permissions`)。
4. 提交代码后，GitHub Actions 会自动进行混淆并发布。
5. 在 `Settings > Pages` 中将分支切换到 `gh-pages`。

## 🔒 代码保护
项目已集成 `javascript-obfuscator`，构建时会自动对 `src/js/main.js` 进行深度混淆。
